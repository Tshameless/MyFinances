[project]
name = "myfinances-python-quant"
version = "0.1.0"
description = "Python-only quant research skeleton for MyFinances."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
